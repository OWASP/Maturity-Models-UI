div.ng-hide(ng-controller='TeamEditController' ng-show='data')
    teamMenu
    br
    .row.fullWidth
        .small-2.columns
            label.right.inline
                b Team Name:
        .small-4.columns
            input#team-name(type="text" ng-model='metadata.team')
        .small-1.columns
            a.button.postfix#save-data(ng-click='save_Data()') save
        .small-1.columns
        .small-4.columns
            .center.label.round#message(ng-class='messageClass') {{status}}

    .row.fullWidth
        hr
    .row.fullWidth
        .medium-3.columns(ng-repeat='(domain, activities) in domains')
            activityTable(title='domain', activities='activities', data='data')
