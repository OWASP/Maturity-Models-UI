doctype html
html(ng-app = "MM_Graph")
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width")

        link(rel="stylesheet", href="/ui/css/app.css")

        script(src='/ui/js/libs.js')
        script(src='/ui/js/angular-src.js')
        script(src='/ui/js/templates.js')

        base(href='/view')

    style.
        .fullWidth {
            width: 100%;
            margin-left: auto;
            margin-right: auto;
            max-width: initial;
        }
    body

        include ./includes/top-navbar.pug
        block content
        //script.
            $(document).foundation();